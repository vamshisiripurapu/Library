use librarysystem;
create table ADDing(bookname varchar(20),author varchar(20));
insert into ADDing values("toystory","keral") ;
select * from ADDing ;
create table viewlib(lid int,lname varchar(20));
select * from viewlib ;
